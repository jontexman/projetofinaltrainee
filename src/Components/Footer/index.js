import React from 'react';

import { Container } from './styles';

export const Footer = () => {
    return (
        <Container>
            <button>Menu</button>
            <button>Menu</button>
            <button>Menu</button>
            <button>Menu</button>
        </Container>
    )
}